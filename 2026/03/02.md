## 取り組んだ課題一覧
 - Docker Udemy応用編
 - 英語学習
 - タイピング
## わかったこと
 - docker-composeコマンドとdockerコマンド
   - docker-compose build = docker build < build contexts >
   - docker-compose up = docker run < image >
   - docker-compose ps = docker ps
   - docker-compose exec < service > < command > = docker exec < container > < command >
 -  便利なコマンド
    - docker-compose up —build：buildしてrun
    - docker-compose down：stopしてrm
## 次やること
 - Docker Udemy応用編
 - クイックスタート
 - 英語学習
 - タイピング
## 感じたこと
クイックスタートのエラーから抜け出せず、気分転換にdocker-composeについてさらに理解を深めるためUdemy応用編を学習した。こちらでもバージョンの違いからエラーにハマっているけど、この苦労を乗り越えた先には間違いなく血肉となっているはずなので頑張る。
## 学習時間
Today:5h26m Total:619h02m